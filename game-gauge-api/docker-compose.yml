version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: gamegauge-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: gamegauge
      POSTGRES_PASSWORD: gamegauge_dev_password
      POSTGRES_DB: gamegauge
    ports:
      - "5555:5555"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U gamegauge"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: gamegauge-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
  redis_data:
